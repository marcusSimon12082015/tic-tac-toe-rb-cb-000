
./lib/tic_tac_toe.rb
  WIN_COMBINATIONS
    defines a constant WIN_COMBINATIONS with arrays for each win combination
  #display_board
    prints arbitrary arrangements of the board
  #input_to_index
    converts user_input to an integer
    subtracts 1 from the user_input
    returns -1 for strings without integers
  #move
    does not allow for a default third argument
    takes three arguments: board, position, and player token
    allows "X" player in the bottom right and "O" in the top left
  #position_taken?
    returns true/false based on position in board
  #valid_move?
    returns true/false based on position
  #turn
    makes valid moves
    asks for input again after a failed validation
  #turn_count
    counts occupied positions
  #current_player
    returns the correct player, X, for the third move
  #won?
    returns false for a draw
    returns true for a win
  #full?
    returns true for a draw
    returns false for an in-progress game
  #draw?
    calls won?
    calls full?
    returns true for a draw
    returns false for a won game
    returns false for an in-progress game
  #over?
    returns true for a draw
    returns true for a won game
    returns false for an in-progress game
  #winner
    return X when X won
    returns O when O won
    returns nil when no winner

./lib/tic_tac_toe.rb
  #play
    asks for players input on a turn of the game (FAILED - 1)

Failures:

  1) ./lib/tic_tac_toe.rb #play asks for players input on a turn of the game
     Failure/Error: expect(self).to receive(:gets).at_least(:once).and_return("1")

       (#<RSpec::ExampleGroups::LibTicTacToeRb_2::Play "asks for players input on a turn of the game" (./spec/02_play_spec.rb:5)>).gets(*(any args))
           expected: at least 1 time with any arguments
           received: 0 times with any arguments
     # ./spec/02_play_spec.rb:10:in `block (3 levels) in <top (required)>'

Finished in 0.03707 seconds (files took 0.24867 seconds to load)
30 examples, 1 failure

Failed examples:

rspec ./spec/02_play_spec.rb:5 # ./lib/tic_tac_toe.rb #play asks for players input on a turn of the game

Coverage report generated for RSpec to /home/marcusSimon12082015/tic-tac-toe-rb-cb-000/coverage. 45 / 51 LOC (88.24%) covered.
